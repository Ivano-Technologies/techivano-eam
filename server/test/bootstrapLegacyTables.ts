/**
 * Vitest setup when DATABASE_URL is set: ensures test environment can load.
 * DB-dependent tests may run migrations or seed in their own beforeAll.
 */
// No-op for now; file must exist so vitest setupFiles resolves.
export {};
